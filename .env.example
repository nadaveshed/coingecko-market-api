# Secrets and external endpoints only. Tuning lives in src/config/defaults.ts.

APP_COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
APP_COINGECKO_API_KEY=

# Enable only when the app is behind a trusted reverse proxy.
APP_TRUST_PROXY=false
